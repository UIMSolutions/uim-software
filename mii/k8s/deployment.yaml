apiVersion: apps/v1
kind: Deployment
metadata:
  name: mii
  namespace: uim-platform
  labels:
    app: mii
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mii
  template:
    metadata:
      labels:
        app: mii
    spec:
      containers:
      - name: mii
        image: uim-platform/mii:latest
        ports:
        - containerPort: 8132
          protocol: TCP
        envFrom:
        - configMapRef:
            name: mii-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
