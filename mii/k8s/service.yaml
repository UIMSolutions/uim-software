apiVersion: v1
kind: Service
metadata:
  name: mii
  namespace: uim-platform
  labels:
    app: mii
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: mii
  ports:
  - name: http
    port: 8132
    targetPort: 8132
    protocol: TCP
