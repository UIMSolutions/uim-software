apiVersion: apps/v1
kind: Deployment
metadata:
  name: mes
  namespace: uim-platform
  labels:
    app: mes
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mes
  template:
    metadata:
      labels:
        app: mes
    spec:
      containers:
      - name: mes
        image: uim-platform/mes:latest
        ports:
        - containerPort: 8132
          protocol: TCP
        envFrom:
        - configMapRef:
            name: mes-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
