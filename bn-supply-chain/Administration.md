# Administration

## Runtime configuration

- Default host: 0.0.0.0
- Default port: 8081

## Notes

- The implementation uses an in-memory repository suitable for demos and tests.
- The repository port can be replaced with a database-backed adapter later without changing the application layer.
