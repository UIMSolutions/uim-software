apiVersion: v1
kind: Service
metadata:
  name: sci
  namespace: uim-platform
  labels:
    app: sci
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: sci
  ports:
  - name: http
    port: 8110
    targetPort: 8110
    protocol: TCP
