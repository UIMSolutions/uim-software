# Spreadsheet Service

This package is a D/vibe.d implementation of a spreadsheet analytics platform inspired by Tableau. It includes:

- a domain model for spreadsheets and metrics
- a hexagonal-style repository port and in-memory adapter
- an HTTP API for CRUD and metrics endpoints
- a simple web client experience
- unit tests for the application use case layer
