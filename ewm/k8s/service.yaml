apiVersion: v1
kind: Service
metadata:
  name: ewm
  namespace: uim-platform
  labels:
    app: ewm
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: ewm
  ports:
  - name: http
    port: 8132
    targetPort: 8132
    protocol: TCP
