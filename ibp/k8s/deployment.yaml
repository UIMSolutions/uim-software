apiVersion: apps/v1
kind: Deployment
metadata:
  name: ibp
  namespace: uim-platform
  labels:
    app: ibp
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ibp
  template:
    metadata:
      labels:
        app: ibp
    spec:
      containers:
      - name: ibp
        image: uim-platform/ibp:latest
        ports:
        - containerPort: 8132
          protocol: TCP
        envFrom:
        - configMapRef:
            name: ibp-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
