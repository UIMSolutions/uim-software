apiVersion: v1
kind: Service
metadata:
  name: ibp
  namespace: uim-platform
  labels:
    app: ibp
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: ibp
  ports:
  - name: http
    port: 8132
    targetPort: 8132
    protocol: TCP
