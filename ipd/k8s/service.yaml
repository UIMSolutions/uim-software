apiVersion: v1
kind: Service
metadata:
  name: epd
  namespace: uim-platform
  labels:
    app: epd
    part-of: uim-platform
spec:
  type: ClusterIP
  selector:
    app: epd
  ports:
  - name: http
    port: 8132
    targetPort: 8132
    protocol: TCP
