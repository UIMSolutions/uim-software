apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecc
  namespace: uim-platform
  labels:
    app: ecc
    part-of: uim-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecc
  template:
    metadata:
      labels:
        app: ecc
    spec:
      containers:
      - name: ecc
        image: uim-platform/ecc:latest
        ports:
        - containerPort: 8132
          protocol: TCP
        envFrom:
        - configMapRef:
            name: ecc-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8132
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
